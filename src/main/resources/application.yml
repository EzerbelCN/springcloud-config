server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git: git@github.com:/EzerbelCN/springcloud-config.git #github上仓库的名字
        search-paths  :
          - springcloud-config
      label: master

eureka:
  client:
    service-url:
      dafaultZone: http://localhost:7001/eureka