server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: git@github.com:EzerbelCN/springcloud-config.git #github上仓库的名字
          search-paths  :
            - springcloud-config
      label: master

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      dafaultZone: http://eureka7001.com:7001/eureka/
